# 代码修改日志

**日期时间**: 2026-04-06

---

## 修改概述

本次修改包含两个阶段：

**第一阶段**：修复代理设置Tab问题 + 添加定时任务功能
**第二阶段**：简化爬虫流程 + 添加历史记录 + 添加JSON导出

---

## 第一阶段修改

### 1. gui/settings_dialog.py
**修改内容**: 修复"大模型代理"和"爬虫代理"Tab显示为空的问题

- 在 `__init__` 中初始化 `self.proxy_enabled_vars = {}` 和 `self._proxy_vars = {}`
- 简化 `_setup_proxy_tab` 方法中的变量存储，改用 `self._proxy_vars` 字典统一管理
- 修复 `_save` 方法中获取代理配置的方式

**关键改动**:
```python
# __init__ 中新增初始化
self.proxy_enabled_vars = {}
self._proxy_vars = {}

# _setup_proxy_tab 中
self._proxy_vars[f'{proxy_type}_protocol'] = protocol_var

# _save 中
self.config.llm_proxy.protocol = self._proxy_vars.get("llm_proxy_protocol").get()
```

---

### 2. config/settings.py
**修改内容**: 新增定时任务配置模型和历史记录配置

- 新增 `ScheduleConfig` 类，支持单次/每日/每周/Cron表达式四种调度类型
- 新增 `HistoryItem` 和 `HistoryConfig` 类，支持历史记录功能
- 在 `AppConfig` 中添加 `schedule: ScheduleConfig` 和 `history: HistoryConfig` 字段

**新增内容**:
```python
class ScheduleConfig(BaseModel):
    """定时任务配置"""
    enabled: bool = False
    schedule_type: str = "once"  # once / daily / weekly / cron
    daily_time: str = "09:00"
    weekly_day: int = 0  # 0=Monday
    weekly_time: str = "09:00"
    cron_expression: str = "0 9 * * *"

class HistoryItem(BaseModel):
    """历史记录项"""
    id: str = ""
    timestamp: str = ""
    task_desc: str = ""
    keywords: list[str] = []
    urls: list[str] = ""

class HistoryConfig(BaseModel):
    """历史记录配置"""
    items: list[HistoryItem] = []
    max_items: int = 6
```

---

### 3. gui/main_window.py
**修改内容**: 添加定时任务功能UI和调度器集成

- 导入新增的 `ScheduleConfig`、`HistoryConfig`、`HistoryItem` 和 `TaskScheduler`
- 在 `__init__` 中初始化调度器并启动
- 新增定时任务UI区域：
  - 启用定时任务复选框
  - 周期选择下拉框（单次/每日/每周/Cron表达式）
  - 时间输入框
  - Cron表达式输入框（仅cron模式显示）
  - 下次执行时间显示标签
- 新增历史记录功能：
  - 历史记录下拉选择框
  - 保存历史记录到配置
  - 加载历史记录自动填充表单
- 新增方法：
  - `_on_schedule_enabled_change()` - 定时任务开关回调
  - `_update_schedule_ui()` - 根据周期类型更新UI
  - `_update_next_run_time()` - 更新下次执行时间显示
  - `_get_schedule_config()` - 获取定时任务配置
  - `_register_scheduled_task()` - 注册定时任务
  - `_execute_scheduled_task()` - 执行定时任务
  - `_execute_task()` - 任务执行入口
  - `_update_history_combo()` - 更新历史记录下拉框
  - `_on_history_select()` - 选择历史记录
  - `_save_to_history()` - 保存到历史记录
  - `_clear_history()` - 清除历史记录
- 修改 `_start_task()` 方法，支持立即执行和定时执行两种模式
- 新增JSON导出功能

---

### 4. utils/scheduler.py
**新建文件**: 定时任务调度器模块

**主要类和方法**:
- `TaskScheduler`: 定时任务调度器类
  - `add_task(name, schedule, callback)` - 添加定时任务
  - `start()` - 启动调度器
  - `stop()` - 停止调度器
  - `remove_task(name)` - 移除任务
  - `get_next_run_time(schedule)` - 获取下次执行时间

**支持的调度类型**:
- `once` - 单次执行
- `daily` - 每日定时
- `weekly` - 每周定时
- `cron` - Cron表达式

---

### 5. crawler/base_crawler.py
**修改内容**: 移除PDF下载功能

- 移除 `PDFDownloader` 导入
- 移除 `pdf_downloader` 初始化
- 移除 `pdfs` 字段
- 移除 PDF下载逻辑

---

### 6. llm/analyzer.py
**修改内容**: 重构System Prompt为4段式结构

**新的System Prompt结构**：
```
你是一个专业的分析助手，负责分析网页内容并生成报告。

【搜索引擎技能】
你可以调用 search(query) 函数搜索网络获取补充信息。
当你认为需要搜索时，直接调用工具，不需要询问用户。
当信息足够完成分析时，输出"完成"。

【输出格式】
请按以下格式输出报告：
# 标题
## 概述
[简要总结关键发现]

## 详细内容
[详细分析，可包含引用来源]
```

---

### 7. requirements.txt
**修改内容**: 新增定时任务依赖

```diff
+ # 定时任务
+ croniter>=4.0.0
```

---

## 第二阶段修改

### 1. gui/main_window.py
**修改内容**:
- 移除搜索引擎下拉选择框（爬虫不再调用搜索）
- 修改 `_run_task()` 方法，只爬取用户指定的URLs
- 新增 `导出JSON` 菜单项和按钮
- 新增 `_export_json()` 和 `_parse_report_to_json()` 方法

---

## 工作流程（最终版）

```
用户输入任务描述 + 关键词 + URLs
    ↓
爬虫直接爬取 URLs 内容（不搜索，不下载PDF）
    ↓
LLM 分析（4段式输入）
    ↓
输出报告
```

---

## 导出格式

| 格式 | 说明 |
|------|------|
| HTML | 带样式的可视化页面，可直接在浏览器打开 |
| Markdown | 纯文本格式，便于编辑和复制 |
| JSON | 结构化数据，便于程序处理 |

---

## 历史记录功能

- 保存最近 **6 次**用户输入记录
- 每条记录包含：任务描述、关键词、URLs、时间戳
- GUI 显示下拉选择
- 点击历史记录自动填充表单
- 清除历史记录按钮

---

## 新建的文件

| 文件 | 说明 |
|------|------|
| utils/scheduler.py | 定时任务调度器模块 |
| SPEC.md | 软件规格说明书 |
| README.md | 用户使用文档 |
| log-2026-04-06.md | 本修改日志 |
| plan-2026-04-06-v2.md | 修改计划文档 |

---

## 项目文件结构

```
fireclaws/
├── main.py
├── requirements.txt
├── config/
│   ├── __init__.py
│   ├── settings.py      # ScheduleConfig, HistoryConfig
│   └── encryption.py
├── crawler/
│   ├── __init__.py
│   ├── base_crawler.py
│   ├── markdown_converter.py
│   └── search_engine.py
├── llm/
│   ├── __init__.py
│   ├── client.py
│   └── analyzer.py     # 4段式System Prompt
├── gui/
│   ├── __init__.py
│   ├── main_window.py  # 定时任务, 历史记录, JSON导出
│   └── settings_dialog.py
├── feishu/
│   └── __init__.py
├── utils/
│   ├── __init__.py
│   ├── logger.py
│   ├── proxy_manager.py
│   └── scheduler.py     # 定时任务调度器
└── data/
    ├── configs/
    ├── downloads/
    └── logs/
```

---

## 验证方式

1. 运行 `pip install -r requirements.txt` 安装新依赖
2. 运行程序
3. 打开设置页面，切换到"大模型代理"或"爬虫代理"Tab，验证内容显示正常
4. 配置代理并保存，验证配置正确保存
5. 在任务页面启用定时任务，设置执行时间，验证下次执行时间显示正确
6. 输入任务描述、关键词、URLs
7. 验证历史记录是否显示在列表中
8. 重新输入，验证历史记录累加（最多6条）
9. 点击历史记录，验证表单自动填充
10. 验证爬虫只爬取指定 URLs（无搜索）
11. 验证 LLM 分析结果正确
12. 验证导出 HTML/Markdown/JSON 正常
