# 代码修改计划 V2

**日期时间**: 2026-04-06

## Context

重构爬虫流程并添加历史记录功能：

1. **保留**：用户输入关键词 + URLs
2. **移除**：PDF 下载动作，只抓取网页内容
3. **修改**：只有 LLM 可以调用搜索引擎（爬虫不调用）
4. **重构**：LLM 输入格式改为4段式
5. **新增**：历史记录功能，保存最近6次用户输入

## 工作流程修改

### 修改后流程
```
用户输入任务描述 + 关键词 + URLs
    ↓
爬虫直接爬取 URLs 内容（不搜索，不下载PDF）
    ↓
LLM 分析（4段式输入）
    ↓
输出报告
```

## 历史记录功能

### 设计
- 保存最近 **6 次**用户输入记录
- 每条记录包含：任务描述、关键词、URLs、时间戳
- GUI 显示下拉选择或历史列表
- 点击历史记录自动填充表单
- 配置持久化到 `data/configs/history.json`

### 数据结构
```python
class HistoryItem:
    id: str  # UUID
    timestamp: str  # ISO格式时间
    task_desc: str
    keywords: list[str]
    urls: list[str]

class HistoryManager:
    items: list[HistoryItem]  # 最多6条
    current_index: int  # 当前选中
```

## 修改的文件

### 1. gui/main_window.py

**新增内容**：
- 历史记录下拉选择框（在任务描述上方）
- 历史记录管理逻辑
- 保存历史记录到配置

**修改内容**：
- `_run_task`：移除搜索引擎调用，只用 URLs 爬取
- 移除搜索引擎下拉选择框

### 2. crawler/base_crawler.py

**移除内容**：
- `PDFDownloader` 导入和使用
- `pdfs` 字段

### 3. llm/analyzer.py

**重构 System Prompt 为4段式**：

```
你是一个专业的分析助手，负责分析网页内容并生成报告。

【任务描述】
{task_desc}

【网页内容】
{content}

【搜索引擎技能】
你可以调用 search(query) 函数搜索网络获取补充信息。
当你认为需要搜索时，直接调用工具，不需要询问用户。
当信息足够完成分析时，输出"完成"。

【分析要求】
{user_prompt}
```

### 4. config/settings.py

**新增内容**：
```python
class HistoryItem(BaseModel):
    id: str = ""
    timestamp: str = ""
    task_desc: str = ""
    keywords: list[str] = []
    urls: list[str] = ""

class HistoryConfig(BaseModel):
    items: list[HistoryItem] = []
    max_items: int = 6
```

## 实施步骤

1. 修改 `config/settings.py`：添加 `HistoryItem` 和 `HistoryConfig`

2. 修改 `gui/main_window.py`：
   - 添加历史记录下拉选择
   - 添加历史记录保存逻辑
   - 修改 `_run_task` 移除搜索引擎调用
   - 移除搜索引擎下拉选择框

3. 修改 `crawler/base_crawler.py`：
   - 移除 PDF 下载相关代码

4. 修改 `llm/analyzer.py`：
   - 重构为4段式 System Prompt

5. 更新 `README.md` 和 `log-日期.md`

## 导出格式

| 格式 | 说明 |
|------|------|
| HTML | 带样式的可视化页面，可直接在浏览器打开 |
| Markdown | 纯文本格式，便于编辑和复制 |
| JSON | 结构化数据，便于程序处理 |

## 验证方式

1. 输入任务描述、关键词、URLs
2. 观察历史记录是否显示在列表中
3. 重新输入，验证历史记录累加（最多6条）
4. 点击历史记录，验证表单自动填充
5. 验证爬虫只爬取指定 URLs
6. 验证 LLM 分析结果正确
7. 验证导出 HTML/Markdown/JSON 正常
