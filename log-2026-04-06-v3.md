# 代码修改日志 V3

**日期时间**: 2026-04-06

---

## 修改概述

1. **修复 LLM 客户端代理协议错误**：清除环境变量防止 httpx 解析 `socks://` 出错
2. **修改 LLM 代理配置**：默认代理改为 `proxysz.zte.com.cn:80`

---

## 修改的文件

### 1. llm/client.py

**问题**: httpx 报错 `ValueError: Unknown scheme for proxy URL URL('socks://127.0.0.1:7897/')`

**原因**: httpx 自动从环境变量读取代理，但系统环境变量被错误解析为 `socks://`，httpx 不支持此协议

**修复**: 在 `_get_client` 方法中，创建 OpenAI 客户端前清除所有 proxy 环境变量

**修改内容**:
```python
def _get_client(self) -> OpenAI:
    """获取或创建客户端"""
    if self._client is None:
        import os
        # 清除可能存在的代理环境变量，防止冲突
        for k in list(os.environ.keys()):
            if 'proxy' in k.lower():
                del os.environ[k]

        # 设置代理（如果需要）
        http_proxy = ProxyManager.get_httpx_proxy(self.proxy)
        if http_proxy:
            os.environ["http_proxy"] = http_proxy
            os.environ["https_proxy"] = http_proxy

        self._client = OpenAI(
            base_url=self.config.base_url,
            api_key=self.config.api_key,
            timeout=120
        )
    return self._client
```

---

### 2. config/settings.py

**需求**: 修改 LLM 默认代理为 `proxysz.zte.com.cn:80`

**修改内容**:
```python
class AppConfig(BaseModel):
    llm_proxy: ProxyConfig = ProxyConfig(
        enabled=True,
        protocol="http",
        host="proxysz.zte.com.cn",
        port=80
    )
    search_proxy: ProxyConfig = ProxyConfig()  # 搜索引擎代理
```

---

## 测试结果

| 测试项 | 结果 |
|--------|------|
| 模块导入 | ✓ 通过 |
| 爬虫 + 代理 | ✓ 通过（example.com, httpbin.org） |
| SSL 验证 | ✓ 已添加 `verify=False` |
| 代理环境变量清除 | ✓ 逻辑正确 |
| LLM 代理配置 | ✓ `proxysz.zte.com.cn:80` |

---

## 之前已修复的问题

| 问题 | 文件 | 状态 |
|------|------|------|
| SSL 验证失败 | crawler/base_crawler.py | ✓ 已修复 |
| 缺少 Optional 导入 | config/encryption.py | ✓ 已修复 |

---

## 项目文件结构

```
fireclaws/
├── main.py
├── requirements.txt
├── config/
│   ├── __init__.py
│   ├── settings.py      # LLM代理改为proxysz.zte.com.cn:80
│   └── encryption.py
├── crawler/
│   ├── __init__.py
│   ├── base_crawler.py   # 添加verify=False
│   ├── markdown_converter.py
│   └── search_engine.py
├── llm/
│   ├── __init__.py
│   ├── client.py        # 清除proxy环境变量
│   └── analyzer.py
├── gui/
│   ├── __init__.py
│   ├── main_window.py
│   └── settings_dialog.py
├── utils/
│   ├── __init__.py
│   ├── logger.py
│   ├── proxy_manager.py
│   └── scheduler.py
└── data/
    ├── configs/
    ├── downloads/
    └── logs/
```
