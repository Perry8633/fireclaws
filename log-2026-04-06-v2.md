# 代码修改日志 V2

**日期时间**: 2026-04-06

---

## 修改概述

1. **移除搜索引擎UI**：爬虫不再调用搜索引擎搜索URLs
2. **移除PDF下载**：只抓取网页内容
3. **LLM 4段式输入**：任务描述 + 网页内容 + 搜索引擎技能 + 分析要求
4. **新增历史记录**：保存最近6次用户输入，支持快速填充
5. **新增JSON导出**：支持导出结构化JSON

---

## 修改的文件

### 1. gui/main_window.py

**新增内容**：
- 历史记录下拉选择框（在任务描述上方）
- 历史记录保存/加载逻辑
- JSON导出功能

**移除内容**：
- 搜索引擎下拉选择框

**修改内容**：
- `_run_task`：移除搜索引擎调用，只爬取用户指定的URLs
- 文件菜单新增"导出JSON"
- 导出按钮新增"导出JSON"

**新增方法**：
- `_update_history_combo()` - 更新历史记录下拉框
- `_on_history_select()` - 选择历史记录
- `_save_to_history()` - 保存到历史记录
- `_clear_history()` - 清除历史记录
- `_export_json()` - 导出JSON
- `_parse_report_to_json()` - 解析报告为JSON

---

### 2. crawler/base_crawler.py

**移除内容**：
- `PDFDownloader` 导入
- `pdf_downloader` 初始化
- `pdfs` 字段
- PDF下载逻辑

---

### 3. llm/analyzer.py

**修改内容**：
- 重构 `SYSTEM_PROMPT` 为4段式结构
- 修改 `analyze` 和 `analyze_simple` 方法的消息构建

**新 SYSTEM_PROMPT 结构**：
```
你是一个专业的分析助手，负责分析网页内容并生成报告。

【搜索引擎技能】
你可以调用 search(query) 函数搜索网络获取补充信息。
当你认为需要搜索时，直接调用工具，不需要询问用户。
当信息足够完成分析时，输出"完成"。

【输出格式】
请按以下格式输出报告：
# 标题
## 概述
[简要总结关键发现]

## 详细内容
[详细分析，可包含引用来源]
```

---

### 4. config/settings.py

**新增内容**：
```python
class HistoryItem(BaseModel):
    """历史记录项"""
    id: str = ""
    timestamp: str = ""
    task_desc: str = ""
    keywords: list[str] = []
    urls: list[str] = ""


class HistoryConfig(BaseModel):
    """历史记录配置"""
    items: list[HistoryItem] = []
    max_items: int = 6
```

**修改内容**：
- `AppConfig` 新增 `history: HistoryConfig` 字段

---

## 工作流程（修改后）

```
用户输入任务描述 + 关键词 + URLs
    ↓
爬虫直接爬取 URLs 内容（不搜索，不下载PDF）
    ↓
LLM 分析（4段式输入）
    ↓
输出报告
```

---

## 导出格式

| 格式 | 说明 |
|------|------|
| HTML | 带样式的可视化页面 |
| Markdown | 纯文本格式 |
| JSON | 结构化数据 |

---

## 历史记录功能

- 保存最近 **6 次**用户输入
- 每条记录包含：任务描述、关键词、URLs、时间戳
- GUI 显示下拉选择
- 点击历史记录自动填充表单
- 清除历史记录按钮

---

## 验证方式

1. 输入任务描述、关键词、URLs
2. 观察历史记录是否显示在列表中
3. 重新输入，验证历史记录累加（最多6条）
4. 点击历史记录，验证表单自动填充
5. 验证爬虫只爬取指定 URLs（无搜索）
6. 验证 LLM 分析结果正确（4段式）
7. 验证导出 HTML/Markdown/JSON 正常
